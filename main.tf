# Stack GKE Autopilot + entrada + manifests — recursos entram na §5.
# Kind / self-hosted não são caminho de entrega da Fase 03.
# Este scaffold existe para CI (fmt/validate) e layout do repo.

locals {
  stack_name = "infra-k8s"
}

apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: tech-challenge
spec:
  type: LoadBalancer
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
